interface:
  display_name: "Scan Secrets"
  short_description: "Scan a repo for secrets and committed leaks."
  default_prompt: "Scan the current repository for sensitive information and check whether any findings have entered Git commits."